# ============================================================
# ⚙️ CONFIGURACIÓN PRODUCCIÓN
# ============================================================
# Las credenciales de base de datos vienen de variables de entorno
# inyectadas por Docker Compose:
# - SPRING_DATASOURCE_URL
# - SPRING_DATASOURCE_USERNAME
# - SPRING_DATASOURCE_PASSWORD

# ============================================================
# ⚙️ CONFIGURACIÓN JPA / HIBERNATE (Producción)
# ============================================================
# No mostrar SQL en producción
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Validación de esquema en producción (más seguro que update)
spring.jpa.hibernate.ddl-auto=validate

# ============================================================
# 🔒 CONFIGURACIÓN DE LOCKS PESIMISTAS (Producción)
# ============================================================
# Timeout más largo para producción
spring.jpa.properties.hibernate.jdbc.lock_timeout=30000
spring.jpa.properties.hibernate.query.timeout=30000

# ============================================================
# 🩺 ACTUATOR (Producción)
# ============================================================
management.endpoint.health.show-details=never

